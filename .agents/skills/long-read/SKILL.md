---
name: long-read
description: "长文精读：收到微信公众号/飞书文档/网页链接或粘贴长文，自动提取正文→结构化拆解→三段式精读摘要→按内容自动选择 ljg 深度链路→输出回飞书群或生成飞书文档。手机端友好，不建文库，不存状态。"
---

# long-read: 长文精读


> ⚠️ **输出流程见 [3] 输出路由。选了 ljg-card 必须先读 [2] ljg-card 执行硬约束**——ljg-card 的唯一产出是 PNG 图片，不是文字描述。

## 工作流概览

```
用户发链接/粘贴长文
  │
  ├─ [0] 来源适配 → 获取正文/拉取仓库
  │    ├─ GitHub 仓库 → learn（深度研究）/ summarize（轻量）→ [3] 输出
  │    └─ 文章类      ↓
  ├─ [0.5] 文体识别 → 专项/默认
  ├─ [0.7] 用户画像参照（判断重要性/长度/金句）
  ├─ [1] 三段式精读摘要（必做）
  ├─ [2] ljg 深度链路判断（自动选择，每次最多 3 条）
  └─ [3] 输出路由 → 群内回复 / 飞书文档
```

---

## [0] 来源适配

| 来源 | 方法 |
|------|------|
| GitHub 仓库 URL（`github.com/<owner>/<repo>`） | 见下方 GitHub 仓库专项 |
| `mp.weixin.qq.com` | 使用 `wechat-article-to-markdown` skill 直接抓取（最快路径，不要用其他方式） |
| 飞书文档 URL（`feishu.cn`/`larksuite.com`） | 使用 `lark-doc` skill 读取 |
| 其他网页 URL | 使用 `read` skill 获取 |
| 纯文本粘贴 | 直接使用 |

### GitHub 仓库专项

识别：URL 形如 `https://github.com/<owner>/<repo>`（含 tree/blob 子路径），或用户发 `owner/repo` 简写。

**处理规则**：
- 判断用户意图：
  - 发仓库链接 + 无额外说明 → 默认**深度研究**，走 `learn` skill 六阶段工作流，用 GitHub MCP 拉取代码和元数据（README、目录结构、核心模块、关键文件），输出结构化报告（项目定位、架构、核心模块、设计意图、关键流程、star 数/活跃度等）
  - 明确说"快速看看""简单总结""README" → 走 `summarize` skill，轻量总结 README 和基本信息
- 深度研究产出较长，生成飞书文档回群
- 轻量总结可直接群内回复
- 仓库内容不做三段式精读摘要（那是给文章用的），按 learn/summarize 的输出格式走

---

获取后检查字数（GitHub 仓库和专项文体不适用此检查）：
- `< 800 字`：跳过完整拆解，直接给一句话摘要（太短不值得精读）
- `≥ 800 字`：走完整流程

---

## [0.5] 文体识别与专项处理

收到内容后，先判断文体类型。如果是以下特殊文体，跳过默认三段式精读 + ljg 深度链路，按专项规则处理。

### 识别流程
```
扫描标题、作者、内容结构
  ├─ 阮一峰/科技爱好者周刊 → 周刊专项
  ├─ 短篇小说/故事体        → ljg-writes 或 ljg-think
  ├─ 深度访谈/Q&A           → ljg-think + ljg-card
  ├─ 数据报告/研报           → ljg-card -i
  └─ 以上都不是              → 走默认流程 [1]→[2]→[3]
```

### 阮一峰《科技爱好者周刊》专项

**识别标志**（满足任一即触发）：
- 标题含"科技爱好者周刊#"、"阮一峰"
- 作者为"阮一峰的网络日志"
- 内容结构含"科技动态""文摘""文章""工具""资源""一句话新闻"等固定小标题

**处理规则**（覆盖默认三段式 + ljg 链路）：

> 周刊是每周固定高频流程，始终基于 USER.md 解析筛选：与用户关注强相关的重点展开，无关的工具/资源/动态直接去掉，不泛泛罗列。

```
📖 《周刊标题》(第X期)

## 🎯 科技动态 · 精选（1~3 条）
从"科技动态""一句话新闻"中挑选与用户最相关的 1~3 条。
用户关注优先级（从高到低）：
  1. AI 大模型 / Agent 能力演进
  2. AI 商业化 / Token 价格
  3. 中国科技 / AI 芯片 / 制造
  4. 工具与生产力 / 工作流
  5. 其他
每条格式：标题 + 一句话摘要 + 为什么值得看

## 📄 文摘（基于画像筛选）
基于 USER.md 判断每条文摘与用户关注的相关度：
- 高相关（AI/Agent/工具/教育/投资/反共识等）：保留原文要点并提炼核心
- 低相关（无关行业/纯信息）：一句带过或直接去掉
保留原文表达，不泛泛改写。

## 💬 言论（全量保留）
逐条保留原文，不筛选、不压缩、不改写。

## 📚 其他章节（只留相关的）
只列与用户关注相关的文章/工具/资源（标题 + 一句话价值），不相关的直接去掉，不凑数。
```

**限制**：
- 不走默认 ljg 深度链路——周刊是杂志体，不需要概念深钻或多维解剖
- 不走默认三段式精读骨架——周刊内容天然碎片化，骨架化没有意义
- 用卡片回复（--as bot，符合 link-card），不生成飞书文档
- **卡片要充实但不硬凑**：周刊重点内容（主题文章、科技动态、文摘、言论）全量囊括，不为压缩丢信息；字数上限约为普通精读卡片的 1.5 倍，不是目标值--重要内容可多说，与用户关注无关的工具/资源直接去掉
- **言论全量保留**：周刊"言论"章节逐条保留原文，不按画像筛选、不压缩、不改写

### 短篇小说专项

**识别标志**：
- 标题含"小说"、"故事"、"虚构"
- 正文以叙事为主，有人物、情节、场景

**处理规则**：
- 不走三段式骨架（小说不能被"核心论点"拆解）
- 优先走 ljg-writes（批评短文）或 ljg-think（主题深钻）
- 输出包含：故事一句话、主题解读、值得细品的段落

### 深度访谈/Q&A 专项

**识别标志**：
- 正文以对话/Q&A 形式为主
- 标题含"对话"、"访谈"、"问答"

**处理规则**：
- 不走三段式骨架
- 优先选 ljg-think（提炼核心判断）+ ljg-card（信息图）
- 输出「核心观点碰撞」：本质是快速总结，只放核心观点 + 金句，不铺陈背景和过程
- 字数硬约束：质量 <8.0 ≤ 200 字；质量 ≥8.0 ≤ 400 字；封顶 600 字，再高也不超过
- 每条观点一句话讲清；金句逐字保留原文，不扩写、不改写、不加「这说明…」式二次解读
- 反常识判断单独拎出，同样一句话，不展开论证
- 宁少勿多：读完记住几个点 + 几句金句即可，不为「完整」堆字

### 数据报告专项

**识别标志**：
- 有大量数据、图表、对比表格
- 标题含"报告"、"研报"、"数据"

**处理规则**：
- 不走三段式骨架
- 优先 ljg-card -i 生成信息图 PNG
- 附一张关键数据表

---


## [0.7] 用户画像（必做：所有精读的解析依据）

精读的目的不是压缩，是为用户筛出**值得进入他脑子**的东西。判断哪些内容重要、哪些金句保留、摘要写多长，需要依据最新的用户画像——用户的关注点、道、法会持续演化，不能依赖写死的快照。

⚠️ **所有精读必须基于用户画像解析，不泛泛而谈。** 内容取舍、详略、金句筛选、摘要长度，都按用户画像判断--不是机械压缩原文，而是为这个具体的人筛出值得进入他脑子的东西。

**画像来源（按优先级）**：
1. **优先用 bridge 每轮注入的 USER.md**（系统提示词中，最新最权威）--默认就用这个，无需读文件
2. bridge 未注入时，用下方兜底快照
3. 需要精确判断时（距上次 >30 天 / 主题高度相关 / 用户要求），动态读取 `learn-x/01_core/道/` 和 `learn-x/01_core/memory/` 下最新 `.md` 文件（`learn-x/` 在 `/Users/yuwei/code/learn-x/`）

从画像中提取：核心关注主题、内在积分牌、世界模型、不感兴趣的噪声类型。快照与最新内容冲突时，**以最新为准**。

**⚠️ ywnext 核心上下文调用（硬检查点，违反即不合格）**

在正文和标题可用后，**必须**调用 `$ywnext 核心上下文` 并按其协议按需选择 `core-context/` 中的静态文件（`full.md` / `weighted.md` / `core.md` 之一，按文章主题与用户画像相关度选择；不取全量 `full-full.md`，它是 ywnext 内部素材）。

按需使用是正确设计：文章与用户主线相关度高时取信息更全的 `full.md`，相关度中等取 `weighted.md`，只需校准判断角度取 `core.md`；不要求每次都用 `full.md`。

它只用于“对我有用分”、阅读优先级和带着什么问题读；纯客观摘要不注入，不替代用户画像，也不改变原文事实判断。

**验证门（进入 [1] 评分前必须自检，未通过不得继续）**：
- [ ] 是否已调用 `$ywnext 核心上下文`？
- [ ] 是否按协议按需选择了 `core-context/` 中的静态文件（`full` / `weighted` / `core` 之一，非全量）？
- [ ] 卡片输出时是否注明「已调用 ywnext 核心上下文（按需：xxx）」？

未调用 ywnext 核心上下文不得进入 [1] 评分。

**兜底画像快照**（上次提炼：2026-W27）：

用户是读书人+诗人+前阿里/字节/小米工程师/架构师，gap 中做价值投资、AI 教育探索、数字游民。内在积分牌是苏轼、孟岩、孔子。

三重世界秩序：
- **原子世界**（底座）：身体/健康、家庭、现金流、真实关系、线下信任
- **比特世界**（网）：信息流动、注意力、连接、信用
- **向量世界**（剑）：AI/模型/语义/知识压缩，AI 增强判断，不替代主体性

高关注主题（精华信号，摘要写长、保留原文金句）：
1. AI 大模型 / Agent 能力演进 / Loop / Project OS / AI 产品理念
2. AI 时代的人何以自处——主体性、判断力、真实行动、不被工具异化
3. AI 商业化 / Token 价格 / AI 落地真实闭环
4. 中国科技 / AI 芯片 / 制造
5. 工具与生产力 / 工作流 / AI 编程
6. 教育——AI 时代教育、园式教育（造园不造笼）、点燃兴趣、因材施教
7. 价值投资（芒格/李录/纳瓦尔）、周期、复利、护城河
8. 创作——写诗、文学、苏轼式生命境界、真实感发
9. 反共识判断、短反馈回路、系统减法（反对高维护伪进展系统）
10. 历史/哲学/文明视角对当下的解释

不感兴趣的信号（一句话带过）：
- 纯融资/PR/产品通稿无深度分析
- AI 套话空话（"AI 将改变一切"类）
- 娱乐八卦、明星热点
- 纯参数堆砌无判断
- 鸡汤、成功学

**金句保留规则**：遇到以下类型的原文句子，**逐字保留原文**（不改写、不压缩），放入「值得记住」：
- 反常识、一击即中的判断句
- 用户可能想直接引用/转发的句子
- 对应当前高关注主题的核心论断
- 有诗意/有力度的表达（苏轼式、芒格式、纳瓦尔式短句）

---

## [1] 三段式精读摘要（必做步骤）

摘要长度**弹性决定**——根据文章质量、深度、与用户关注主题的匹配度决定写多长，不要把高质量内容压碎：

- **短文/水文/通稿**（< 1500 字 或 无深度）：一句话 + 骨架（≤3 条）+ 0~1 条值得记住，总计约 150 字以内
- **中等质量文章**（常规科技/工具/观点文）：一句话 + 骨架（3~4 条）+ 2~3 条值得记住，总计 150~350 字
- **高质量长文/重要文章**（深度分析、一手访谈、用户高关注主题的核心文章、8000 字以上好文章）：一句话 + 骨架（4~6 条）+ 3~5 条值得记住（含原文金句），总计 300~600 字
- **特别重要的文章**（直接击中用户核心关切，或经典级文章）：可到 800 字，但每句话都必须有信息增量，不要注水

**双维度评分**（放在摘要最前面，一眼判断读不读、读多深）：

维度一：**文章质量分**（先看内容本身的完成度、深度、逻辑、信息密度；然后叠加来源加权——不以是否原创简单加减分，但一手亲历者的判断力密度天然高于二手观察）：

**内容基础分**（三个子维度分别打分，每个 0.5 粒度，再取平均，保留一位小数）：

| 子维度 | 测什么 | 10 分长什么样 |
|--------|--------|-------------|
| 完成度 | 文章是否自洽？论点是否有充分论证支撑？有没有关键环节缺失？ | 每个论点都有证据/例子/数据支撑，逻辑闭环，读完不觉得「这里没展开」 |
| 深度 | 触及本质还是浮在表面？有没有反常识判断？有没有超越行业共识的洞察？ | 读完后对这件事的理解框架被改变了，不是「知道更多」而是「重新理解」 |
| 逻辑 | 论证链条是否严密？因果是否清晰？有没有偷换概念或逻辑跳跃？ | 每段推理都可以追溯，反驳也能预判，读起来像数学证明一样干净 |

**基础分 = (完成度 + 深度 + 逻辑) ÷ 3**，保留一位小数（如 8.3、7.8、9.0）。

**注水惩罚**（基于信息密度，只做负向，不进基础分）：

信息密度不再作为正面拉分项--「深度」已覆盖"有没有反常识洞察"，洞察密度本质就是信号密度，正面拉分与之重叠。它现在只用来识别注水文：正常不扣分，只有为凑篇幅而注水才扣。

| 注水程度 | 扣分 | 典型 |
|---------|------|------|
| 不注水 / 正常 | 0 | 每段有新信息 |
| 轻度注水 | -0.5 | 正确的废话较多，但骨架清晰 |
| 明显注水 | -1.0 | 5000 字挑 1 个观点，大量重复套话 |
| 严重注水 | -1.5 | 通稿/水文，几乎全是正确的废话 |

**⚠️ 不算注水的情形**：报告体（大量案例/引文/情绪描述天然稀释信息密度）、访谈体（口语稀释）、随笔体（氛围即内容）的"信息密度偏低"是文体特征，不是注水，一律不扣分。只有为凑篇幅注水才扣。

**调整后基础分 = 基础分 + 注水惩罚**（注水惩罚 ≤ 0），再叠加来源加权得最终质量分。

**⚠️ 粒度硬性约束（最高优先级，违反即不合格）**：
- 每个子维度必须精确到 0.5（允许的值：0.5、1.0、1.5、2.0……9.5、10.0），**禁止使用 7、8、9 等裸整数**（应写 7.0、8.0、9.0）
- 注水惩罚粒度 0.5，范围 0 ~ -1.5，正常为 0
- 最终质量分保留一位小数，**禁止四舍五入到整数**——8.3 就是 8.3，不要写成 8；8.9 就是 8.9，不要写成 9
- 8.3 和 9.3 是截然不同的文章质量——不要因为「看起来差不多」就合并
- **评分后必须自查**：三个子维度（完成度、深度、逻辑）是否都精确到了 0.5？如果全是 X.0 没有 X.5，说明粒度不够细，至少两个子维度应带 .5

**基准参考**（帮助校准，基础分 = 三维 ÷ 3，再叠加注水惩罚）：
- 钟十六 Loop 文：完成度 8.5、深度 8.5、逻辑 8.0 -> 基础 8.3，注水 0 -> 调整后 8.3
- 李开复访谈：完成度 8.5、深度 8.5、逻辑 8.0 -> 基础 8.3，注水 0 -> 调整后 8.3
- 普通科技教程/工具文：完成度 7.0、深度 6.5、逻辑 7.0 -> 基础 6.8，轻度注水 -0.5 -> 调整后 6.3
- AI 通稿/软文：完成度 5.0、深度 4.0、逻辑 5.0 -> 基础 4.7，明显注水 -1.0 -> 调整后 3.7
- 水文/标题党：完成度 3.0、深度 2.0、逻辑 3.0 -> 基础 2.7，严重注水 -1.5 -> 调整后 1.2

**来源加权**（0~1.5 分，粒度 0.1，在基础分上叠加，上限 10 分）：

来源加权衡量的是「作者为这个判断付出了什么代价」——不是写得对不对，而是判断背后有没有真金白银、真人真事、真实决策在背书。

| 判断力密度 | 加权 | 典型场景 |
|-----------|------|---------|
| 纯第三方观察（记者、分析师、转述者） | 0 | 行业报道、采访整理、二手综述 |
| 行业从业者，有一定一手经验，但非核心决策者 | 0.3~0.5 | 中层管理者分享、从业者经验总结 |
| 创始人/CEO，一手亲历，但决策规模或深度有限 | 0.6~0.8 | 创业公司创始人分享赛道经验、失败反思 |
| 核心决策者，重大战略判断，真金白银的代价 | 0.9~1.2 | 大厂高管/知名创始人，涉及数亿级决策、大规模裁员、赛道转向 |
| 顶级决策者，范式级判断，改变行业格局 | 1.3~1.5 | 李开复放弃万亿参数基模（裁掉大批研究员）、张一鸣级战略转向 |


**特例**（优先于通用表，按公众号/作者匹配）：

最终质量分 = 调整后基础分 + 来源加权，上限 10 分（调整后基础分 = 基础分 + 注水惩罚）。
|-----------|------|------|
| 刘敏的咖啡馆 / 十分吸引 | 0.8~1.0 | 播客主理人，深度独立判断，涉及哲学/投资/AI 跨领域洞察，判断力密度接近核心决策者 |

**判断标准**：问自己——「这个人说这句话，是用什么换的？」如果只是用时间换的（采访、调研），加权低；如果是用钱、人、公司命运换的，加权高。

最终质量分 = 内容基础分 + 来源加权，上限 10 分。


**⚠️ 来源加权禁止默认值（最高优先级）**：
- 来源加权必须根据作者身份和判断力密度对号入座，**禁止不分析作者就直接给 1.0**
- 大多数文章的作者是记者/分析师/转述者 → 加权 0
- 加权 0 是合法且常见的结果，不要因为「看起来不像零」就强行给一个非零值
- 评分后必须自查：是否看了作者是谁？是否认真对照了上方的判断力密度表？

**评分输出格式**（必须在摘要中显式写出，让评分过程可追溯）：
`质量 9.5/10（完成度 8.5 + 深度 8.5 + 逻辑 8.0 → 基础 8.3 - 注水惩罚 0 + 来源加权 +1.2 = 9.5）`
`> 来源加权 +1.2：一句话理由`

**来源加权为什么重要**：同一篇关于企业 AI 转型的文章，记者写的和创始人写的，在「完成度」上可能同分，但后者每句话都是真金白银换的。李开复说「放弃万亿参数基模」——这不是分析，是 2025 年他亲手裁了一大批基模研究员之后做的决定，来源加权可达 1.2~1.3。而酱油作为 AI 漫剧创业公司创始人，亲历行业从风口到消亡，但判断的代价和格局不同，加权约 0.6~0.8。这个差异是合理的——不是所有「一手亲历」都等价。

维度二：**对我有用分**（结合用户画像和当前关注的主题判断对"这个用户"的价值——同一个内容对不同人有用度完全不同）：
- ★★★★★：直接命中当前项目/近期思考/反复问过的问题，读完立刻能用
- ★★★★：和当前关注领域强相关，有可借鉴的思路或方法
- ★★★：有一定参考价值，但不是当前优先事项
- ★★：有点意思但暂时用不上
- ★：无关内容，可跳过

**评分→深度链路数量（按最终质量分和"对我有用分"取高者决定）：**
- 质量 ≤4.9 或 有用 ≤★：不调用 ljg，一句话带过
- 质量 5.0~6.9 或 有用 ★★：0~1 条 ljg
- 质量 7.0~7.9 或 有用 ★★★：1~2 条 ljg（视内容饱满度决定）
- **质量 8.0~8.9 或 有用 ★★★★：2 条 ljg，其中至少 1 条是 ljg-card**（另一条按内容特征选 ljg-think / ljg-qa 等）
- **质量 ≥9.0 或 有用 ★★★★★：3 条 ljg，其中至少 1 条是 ljg-card**（其余 2 条按内容特征互补搭配）

质量 4.9 以下压到 100 字以内。评分完整格式见下方硬约束。

**⚠️ 评分格式硬约束（违反即不合格，最高优先级）**：
- **禁止只写裸数字**（如 `质量 9.0/10`）。评分必须显式拆解三子维度 + 注水惩罚 + 来源加权，格式如下：
  `质量 8.9/10（完成度 8.5 + 深度 8.5 + 逻辑 8.0 -> 基础 8.3 - 注水惩罚 0 + 来源加权 +0.6 = 8.9）`
- 三个子维度（完成度、深度、逻辑）缺一不可，各 0.5 粒度；注水惩罚必须显式写出（即使为 0）
- 来源加权必须显式写出数值（等式内）；来源加权 >0 时用 `>` 引用块给出一句话理由（对照来源加权表），为 0 时省略
- **没有拆解过程的评分 = 没有评分**，后续 ljg 链路无法基于裸数字判断

**结构**：

```
📖 《文章标题》

【评分】质量 9.5/10（完成度 8.5 + 深度 8.5 + 逻辑 8.0 → 基础 8.3 - 注水惩罚 0 + 来源加权 +1.2 = 9.5）· 有用度 ★★★★★
> 来源加权 +1.2：一句话理由

【一句话】这篇文章在说什么——20~50 字，说清核心论断或独特贡献，不要写"本文介绍了……"这种空话

【骨架】
- 论点 1：一句话核心（≤50 字，一行）
- 论点 2
- ……
（短文 ≤3 条，中等文 3~4 条，高质量文 4~6 条；每条一行短句，只点核心论点，禁止展开成段落）

【值得记住】
- 🔢 关键数据/例子（带原文引用）
- 💡 反常识判断/核心论断（逐字保留原文金句，不要改写）
- 🛠 可带走的方法论/判断框架
- ✍️ 有力度的原文金句（逐字保留）
（短文 ≤2 条，中等文 2~3 条，高质量文 3~5 条；每条一行，金句逐字保留，其他一句话不展开）
```

**约束：**
- **精简优先，宁短勿长**：每条信息只说核心。论点不展开论证，背景不铺陈，例子/数据只在"值得记住"里给。骨架保持一行短句，正文不出现长段落
- **禁止编造**：不编造原文不存在的观点、数据、例子；金句必须逐字核对原文
- **加粗规则：** 骨架中的核心概念、值得记住中的关键句/金句、方法论关键字，必须加粗
- **段落长度：** 骨架每条 ≤50 字一行；一句话摘要 ≤40 字；正文单段不超过 60 字，超过即拆。禁止把骨架或摘要写成整段文字
- **骨架风格（强约束）：** 每条一行短句，只点核心论点，不展开论证。❌ `- **参数与开源**：2.8 万亿参数、百万上下文，7 月 27 日全面开源。是国内首个 3 万亿级...（多句段落）` ✅ `- **参数与开源**：2.8T 参数、7 月开源，回击 Scaling Law 见顶论`
- **禁止冗余说明：** 评分一行写完（含来源加权数值），不在评分后另写"来源加权说明""有用度说明"等解释段落；如需说明并入评分括号一句话带过
- **金句逐字保留：** 符合金句保留规则的原文句子，必须用引号或引用格式逐字保留，不得改写、压缩、转述
- **判断标准：** 问自己--"用户读完这条摘要，是否能知道这篇文章值不值得深读？哪些句子他可能想划出来？"如果摘要读完感觉什么都没说，那就是写得太短了
- **飞书排版增强（强约束）：** 骨架和要点用 `-` 飞书原生无序列表（不用 `▸`、`•` 等自定义符号）；数据对比、多维评分等结构化信息可用表格（≤4 列，确保手机可读）；来源加权理由用 `>` 引用块一句话；正文少用长段落，多用列表和短句

评分完成后按上方映射表选 ljg 链路；≥8.0 分必须含 ljg-card，条数不足需有充分理由（不是「不确定」「省 token」）。

---

## [2] ljg 深度链路（自动判断）

扫描摘要骨架，判断内容最适合哪条深度链路。每次最多选 **3 条**（互补搭配，见下文搭配策略）。

### ⚠️ ljg-card 强制规则（最高优先级，覆盖匹配规则）

**质量分 ≥8.0 的文章，ljg 链路组合中必须包含 `ljg-card`（信息图），不论内容特征是否匹配「多角色/利益方/分层」。**

理由：8 分以上的好文章值得一张视觉化产出，不能因为「内容偏观点、无明显可视化素材」就跳过 ljg-card。ljg-card 的模具选择（`-i` 信息图 / `-l` 长图）仍按内容特征判断，但 ljg-card 本身不可省略。

与 ljg 条数规则配合：
- 质量 8.0~8.9：2 条 ljg，其中至少 1 条是 ljg-card，另一条按内容特征选
- 质量 ≥9.0：3 条 ljg，其中至少 1 条是 ljg-card，其余 2 条按内容特征互补搭配

如果质量分 ≥8.0 但 ljg-card 生成失败（playwright 报错、上传失败等），按下方「ljg-card 执行硬约束」的失败处理--标记失败并说明原因，不能静默跳过，也不能用文字描述替代 PNG。

### 匹配规则

| 内容特征 | 匹配链路 | 输出形式 |
|-----------|---------|---------|
| 提出一个核心概念/理论/术语 | `ljg-think` — 钻到根 | 思考链短文 |
| 覆盖多个维度或层次/可解剖 | `ljg-learn` — 八维解剖 | 结构洞察 |
| 争议性论述/可辩论 | `ljg-roundtable` — 多角度碰撞 | 辩论链 |
| 反常识/值得批判的立场 | `ljg-writes` — 批评短文 | 1500 字短文 |
| 逻辑链长/层层递进 | `ljg-qa` — 问答链 | QA 链 |
| 有多个角色/利益方/行业分层，适合可视化 | `ljg-card` — 信息图/长图 ⚠️ 见下方执行硬约束，必须生成 PNG | PNG 图片 |
| 以上都不明显 | 不调用 ljg | 纯摘要 |

**判断规则：**
- 先扫骨架，看有没有一个突出的**核心概念**或**独特术语** → `ljg-think`
- 如果文章有多维度展开（经济学/心理学/历史等多角度） → `ljg-learn`
- 如果文章有争议性立场或多个利益相关方视角 → `ljg-roundtable`
- 如果文章在清晰解释一个罕见概念/单词 → `ljg-word`
- **不堆砌**--优先选 1-2 条（但当质量分 ≥9.0 时，3 条是硬性要求，不是可选）
- **不确定时不调**--纯摘要已经足够（仅适用于质量分 <8.0 的可选区间；≥8.0 分时 ljg 条数是强制的，不得以「不确定」为由跳过）

> ⚠️ **选中 ljg-card 时，跳过上方判断规则，直接跳到下方「ljg-card 执行硬约束」。ljg-card 的唯一产出是 PNG 图片，不是文字描述。图片作为原生图片块插入飞书文档，不直接发到群里。**

### ⚠️ ljg-card 执行硬约束（最高优先级，违反即不合格）

**ljg-card 的价值是视觉输出。只写文字描述 = 没执行 ljg-card。**

**text-only bridge 边界**：PNG 只是交付文件，不是模型输入。禁止调用 `view_image`、Browser/Computer 截图或其他会把像素返回模型的工具；只允许用 `capture.js` 在本地生成 PNG，并通过 HTML/DOM、文件尺寸和 mtime 做文本验收。

选中 ljg-card 时，必须在写飞书文档内容之前完成以下步骤：

#### 执行步骤（必须逐条执行，不可跳过）

1. **判断模具**：按内容特征选模具。行业/角色/利益方/分层 → `-i`（信息图）；单篇长文 → `-l`（长图）；纯数据/报告 → `-i`
2. **Read ljg-card SKILL.md**：先读取 `/Users/yuwei/.codex/skills/ljg-card/SKILL.md` 确认参数和模具
3. **Read 对应 mode 文件**：按选定的模具读取 references 下的 mode 文件（如 `mode-infograph.md`）
4. **过品味底线**：按 ljg-card SKILL.md「品味准则」节逐项排查（底线已内联进 SKILL.md）
5. **生成 HTML**：按 mode 文件步骤写 HTML，写入 `/tmp/ljg_cast_{name}.html`
6. **截图**：
   ```bash
   cd /Users/yuwei/.codex/skills/ljg-card && node assets/capture.js /tmp/ljg_cast_{name}.html ~/Downloads/{name}.png 1080 800 fullpage
   ```
   - 必须在 ljg-card skill 根目录下执行（capture.js 依赖相对路径的 node_modules/playwright）
   - 如果 playwright 报错，先执行 `cd /Users/yuwei/.codex/skills/ljg-card && npm install`
7. **验证文件存在**：`ls -la ~/Downloads/{name}.png` 确认文件大小 > 0
8. **在飞书文档中插入图片块**（建文档后执行，见 [3] 路径 C 步骤 4）：用 `docs +media-insert` 把 PNG 作为原生图片块插入文档。**禁止用 `drive +upload` + internal-api-drive-stream URL 嵌入 markdown**--那条路径 doc 导入服务鉴权稳定返回 401，已确认不可用。
   ```bash
   cd ~/Downloads && lark-cli docs +media-insert --doc <文档URL或doc_id> --file ./{name}.png --type image --align center --caption "📊 信息图：{一句话标题}"
   ```
   - `--file` 必须相对路径，先 `cd` 到图片所在目录
   - 默认 append 到文档末尾；caption 带「📊 信息图」前缀作章节标识，ljg-card 产出就是文档末尾这张图
   - **不要用 `--selection-with-ellipsis`**--当前 lark-cli 1.0.65 下报 `invalid token`（code -32602），不可用；定位章节靠文档结构（ljg-card 放最后）+ caption
   - 返回 `ok: true` + `block_id` 即成功；图片通过 docx image block API 绑定（建块->上传->绑定 token），绕开 401
   - 这一步在 `docs +create` 建完文档之后执行，不是之前

#### 禁止事项

- 禁止只写文字描述（如「这里应该有一张信息图」）替代实际生成
- 禁止在飞书文档里用文字描述替代 PNG——文档里必须嵌入图片
- 禁止跳过品味准则直接生成——不经品味校准的图是废图

#### 验证门（选了 ljg-card 后必须自检）

在进入 [3] 输出路由之前，逐条确认：

- [ ] 是否已读取 `/Users/yuwei/.codex/skills/ljg-card/SKILL.md`？
- [ ] 是否已读取对应 mode 文件（如 `mode-infograph.md`）？
- [ ] 是否已过 ljg-card SKILL.md 品味准则底线？
- [ ] 是否已生成 HTML 文件到 `/tmp/ljg_cast_*.html`？
- [ ] 是否已在 ljg-card skill 根目录执行 `capture.js`？
- [ ] 是否已确认 `~/Downloads/{name}.png` 存在且大小 > 0？
- [ ] 是否已在建文档后用 `lark-cli docs +media-insert` 把 PNG 作为图片块插入文档（返回 ok:true）？

**如果任何一项未完成，不要进入 [3] 输出路由。** ljg-card 没有「部分完成」——要么生成 PNG 上传云空间并嵌入文档，要么标记失败并说明原因。


### Skill 搭配策略


**互斥搭配（不要同时选）：**
- `ljg-think` + `ljg-learn`（重叠太多，都是结构分析）
- `ljg-roundtable` + `ljg-qa`（都偏逻辑链，选一个即可）
- `ljg-word` + 其他（ljg-word 太专，独立使用）

多 Skill 产出时需合并到同一篇飞书文档，摘要在前，skill 各占一个大标题。

### ljg 链路完成自检（进入 [3] 前）

- [ ] ywnext 核心上下文已调用（[0.7] 硬检查点），卡片已注明调用情况
- [ ] 评分是完整拆解格式（非裸数字），ljg 条数符合映射表，≥8.0 分含 ljg-card
- [ ] ljg-card 已完成（PNG 上传云空间并嵌入文档）或已标记失败

---

## [3] 输出路由

| 条件 | 输出方式 |
|------|---------|
| 无 ljg 产出 + 总长度 ≤ 15 行 | 群内直接回复（markdown） |
| 无 ljg 产出 + 总长度 > 15 行 | 生成飞书文档 → 立即群里 + 私聊各发一份完成卡片 |
| 有 ljg 产出（不含 ljg-card） | 生成飞书文档 → 立即群里 + 私聊各发一份完成卡片 |
| 有 ljg-card 产出 | 先生成 PNG -> 建飞书文档 -> `docs +media-insert` 插图片块 -> 群里 + 私聊各发一份完成卡片 |

**⚠️ 飞书 bridge 环境强制规则：创建完飞书文档后，必须在同一轮里自动把完成卡片发到原群（`--chat-id <bridge_context.chatId>`）+ 私聊发给用户本人（`--user-id <bridge_context.senderId>`），绝不等用户催。p2p 场景只发一次。**

**⚠️ 会话重置强制规则：精读输出完成（卡片已发送）后，必须以 user 身份发送 `/new` 重置会话**——避免上下文无限累积导致模型推理卡死。详见下方「会话重置」小节。

### 输出流程

#### 路径 A：群内直接回复（短摘要，无 ljg，≤15 行）

1. 群内直接回复 markdown 摘要
2. 将摘要内容按 link-card 卡片模板构建卡片 JSON，写入 `/tmp/link_card.json`
3. 清理 `/tmp/link_card.json`

#### 路径 B：生成飞书文档 + 群里 + 私聊各发一份卡片（长摘要 / 含 ljg，不含 ljg-card）

**飞书文档排版要求（硬约束）**：生成飞书文档时充分利用飞书原生排版能力增强可读性：多用 `-`/`1.` 列表少用长段落；结构化对比/数据用表格（≤4 列，手机可读）；金句/来源说明用 `>` 引用块；输出精简抓重点不铺陈。详见 [1]「飞书排版增强」约束。

1. 写完精读内容到本地临时 markdown 文件
2. 用 `lark-cli docs +create --title "[精读] 《原文标题》" --content @.wx_doc.md --doc-format markdown --parent-position my_library` 创建内网文档（必须在项目根目录 cwd 下执行，用相对路径 @ 引用），取返回的文档链接
3. 按 link-card 卡片模板构建完成卡片 JSON，写入 `/tmp/link_card.json`
4. 两边发卡片：先 `lark-cli im +messages-send --as bot --chat-id <bridge_context.chatId> --msg-type interactive --content "$(cat /tmp/link_card.json)" --format json` 发到原群，再 `lark-cli im +messages-send --as bot --user-id <bridge_context.senderId> --msg-type interactive --content "$(cat /tmp/link_card.json)" --format json` 私聊发给用户本人。p2p 场景只发一次。
5. 清理临时文件（`.wx_tmp.md`、`.wx_doc.md`、`/tmp/link_card.json`）
6. 文档内容：三段式摘要在前，各 ljg 产出分节在后，末尾附原文章链接


当 ljg 链路中包含 ljg-card 时，先生成 PNG，建文档后用 `docs +media-insert` 插入图片块：

1. **按 ljg-card 执行硬约束步骤 1-7 生成 PNG**（步骤 8 在建文档后执行）
2. 写完精读内容 + 其他 ljg 产出到 `.wx_doc.md`（**不要写 `## 📊 信息图` 章节，也不要用 markdown 图片语法**--ljg-card 产出靠步骤 4 的 media-insert 把图片 append 到文档末尾，caption 自带「📊 信息图」标识；排版要求同路径 B）
3. 用 `lark-cli docs +create --title "[精读] 《原文标题》" --content @.wx_doc.md --doc-format markdown --parent-position my_library` 创建内网文档，取返回的文档链接/doc_id
4. **插入图片块**（即 ljg-card 执行硬约束步骤 8）：`cd ~/Downloads && lark-cli docs +media-insert --doc <doc_id> --file ./{name}.png --type image --align center --caption "📊 信息图：{标题}"`，返回 `ok:true` 即成功（默认 append 到文档末尾；不要用 `--selection-with-ellipsis`，当前版本报 invalid token）
5. 按 link-card 卡片模板构建完成卡片 JSON，写入 `/tmp/link_card.json`
6. 两边发卡片：先 `lark-cli im +messages-send --as bot --chat-id <bridge_context.chatId> --msg-type interactive --content "$(cat /tmp/link_card.json)" --format json` 发到原群，再 `lark-cli im +messages-send --as bot --user-id <bridge_context.senderId> --msg-type interactive --content "$(cat /tmp/link_card.json)" --format json` 私聊发给用户本人。p2p 场景只发一次。
7. 清理临时文件（`.wx_tmp.md`、`.wx_doc.md`、`/tmp/link_card.json`、`/tmp/ljg_cast_*.html`）

**⚠️ 路径 C 的关键约束**：
- 图片用 `docs +media-insert` 作为原生图片块插入，**禁止用 `drive +upload` + internal-api-drive-stream URL 嵌入 markdown**--那条路径 401 稳定复现
- PNG 必须在建文档前生成完毕；图片插入在建文档后执行
- 如果 PNG 生成失败，标记 ljg-card 为「生成失败」，继续走路径 B 处理其他 ljg 产出
- 完成卡片中不需要注明图片信息（图片已在文档里）
- 如果 media-insert 失败，文档文字部分仍完整（未预写空章节），标记 ljg-card 失败即可，不需额外清理文档

### 会话重置（所有路径必做，最后一步）

精读输出完成（卡片已发送）后，必须立即以用户身份发送 `/new` 重置会话，避免上下文无限累积导致模型推理卡死：

```bash
lark-cli im +messages-send --as user --chat-id <bridge_context.chatId> --text "/new" --yes
```

**为什么必须用 user 身份**：bot 自己发的 `/new` 会被 bridge 当回声忽略，只有 user 身份发的才会被识别为命令并触发 `sessions.clear`。

**时序安全**：这条 `/new` 会排队，等当前精读 run 自然结束后才被 bridge 处理，不会打断精读。bridge 会回复「已开始新会话」，下一轮对话就是干净的新 session。

**代价**：重置后这篇精读的上下文 agent 不再记得，用户想追问需引用飞书文档（产出已存档）。这是用户明确选择的取舍——每篇都清，优先避免卡死。


完成卡片格式（markdown，控制在 10 行以内）：

**不含 ljg-card 时**：
```
**精读完成 ✅**

📄 <飞书文档链接>

- **评分：质量 9.5/10（完成度 8.5 + 深度 8.5 + 逻辑 8.0 → 基础 8.3 - 注水惩罚 0 + 来源加权 +1.2 = 9.5）· 有用度 ★★★★★**——一句话说明为什么是这个分
- **X 条 ljg 链路**：`ljg-xxx`（一句话说挖了什么）+ `ljg-xxx`（一句话）
- **冷抓取 Xs，总耗时约 Xs**
```

**含 ljg-card 时**（图片已嵌入飞书文档）：
```
**精读完成 ✅**

📊 信息图已嵌入飞书文档
📄 <飞书文档链接>

- **评分：质量 9.5/10（完成度 8.5 + 深度 8.5 + 逻辑 8.0 → 基础 8.3 - 注水惩罚 0 + 来源加权 +1.2 = 9.5）· 有用度 ★★★★★**——一句话说明为什么是这个分
- **X 条 ljg 链路**：`ljg-card`（📊 信息图）+ `ljg-xxx`（一句话）
- **冷抓取 Xs，总耗时约 Xs**
```

---

## 完整输出示例

### 短摘要（群内直接回）

```
📖 《为什么伟大不能被计划》

【评分】质量 7.5/10（完成度 7.5 + 深度 7.5 + 逻辑 7.5 → 基础 7.5 - 注水惩罚 0 + 来源加权 0 = 7.5）· 有用度 ★★★★

【一句话】伟大成果来自好奇心探索，不是目标驱动

【骨架】
- 目标导向扼杀意外发现
- 好奇心不是干扰，是燃料
- 伟大发现往往是"走偏"的结果
- 人类直觉低估了偶然性的价值

【值得记住】
- 🔢 OpenAI 的创始动机不是为了做 GPT，而是研究 AGI 的"旁边"
- 💡 你越盯着一个目标，越看不见通往它的捷径
- 🛠 不要问"这个对我的目标有用吗"，问"这个有意思吗"
```

### 长摘要（生成飞书文档后在群内发）

```
📖 《文章标题》

已生成精读笔记：[链接]

一句话：……（核心概念加粗）
```

---

---

## 与现有 Skill 的关系

| 阶段 | 调用 | 说明 |
|------|------|------|
| 微信抓取 | `wechat-article-to-markdown` | 现成，**默认唯一路径** |
| 飞书文档读取 | `lark-doc` | 现成 |
| 网页读取 | `read` | 现成 |
| GitHub 仓库深度研究 | `learn` | 现成，六阶段工作流 |
| GitHub 仓库轻量总结 | `summarize` | 现成，README + 基本信息 |
| 飞书文档创建 | `lark-doc` | 现成 |
| 深度链路 | `ljg-think` / `ljg-learn`/ `ljg-roundtable` / `ljg-writes` / `ljg-qa` / `ljg-word` | 现成，自动选择 |
